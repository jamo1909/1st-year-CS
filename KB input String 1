/*
Write a Java program that creates a String variable from user input and replaces every instance of a lowercase 'a' with '#' if 'a' appears 3 times or less. Otherwise replace 'a' with a '@'. Then print this new String to the screen. 
*/

public class While
{
    public static void main (String [] args)
    {
        int i = 0-1;
        
        while (i < 500)
        {
            i++;
            if (i%5==0 && i%7==0 )
            {
                System.out.println (i + "is a multiple of 5 and 7");
            }
            else if (i%5==0 || i%7==0)
            {
                System.out.println (i + "is a multiple of 5 or 7");
            }
        }
    }
}
