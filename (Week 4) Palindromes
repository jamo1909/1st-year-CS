/*
Write a Java program that will determine if a String, called text, is a palindrome. A String is a palindrome if the word remains the same when spelled in reverse 
For example: 
NAVAN is a palindrome 
CAVAN is NOT a palindrome
*/

public class Palindrome{
    public static void main(String [] args)
    {
        String str1 = "navan";
        String str2 = "";
        
        for(int i = str1.length()-1; i>=0;i--)
        {
            str2 += str1.charAt(i);
        }
        
        if(str1.equals(str2)){
            System.out.println(str1.toUpperCase() + " is a palindrome");
        }else{
            System.out.println(str1.toUpperCase() + " is NOT a palindrome");
        }
        
    }
}
